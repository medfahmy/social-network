{
  "scripts": {
    "b": "npm run server --prefix api",
    "f": "npm start --prefix client",
    "client-install": "npm install --prefix",
    "dev": "concurrently \"npm run b\" \"npm run f\" "
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
